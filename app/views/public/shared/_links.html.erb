<%- if controller_name != 'sessions' %>
  <div class="container">
    <div class="row">
      <h2>既に登録済みの方</h2>
    </div>
    <div class="row">
      <p class="pe-3"><%= link_to " こちら", new_session_path(resource_name) %>からログインしてください。</p>
    </div>
  </div>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <div class="container">
    <div class="row">
      <h3>会員登録がお済みでない方</h3>
    </div>
    <div class="row">
      <p><%= link_to "こちら", new_registration_path(resource_name) %>から新規登録を行なってください。</p>
    </div>
  </div>
<% end %>

